export const LANGUAGES = { vba:'VBA', excel:'Excel Formula', javascript:'JavaScript', python:'Python', sql:'SQL', html:'HTML', css:'CSS', json:'JSON', powershell:'PowerShell', batch:'Batch', text:'Text', other:'Other' };
export const escapeHtml = (value = '') => String(value).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export const previewCode = (code = '', max = 11) => { const lines = code.split(/\r?\n/); return { text: lines.slice(0,max).join('\n'), clipped: lines.length > max }; };
export const debounce = (fn, ms=900) => { let timer; return (...args) => { clearTimeout(timer); timer=setTimeout(()=>fn(...args),ms); }; };
export function relativeDate(value) { const d=new Date(value), now=new Date(), diff=now-d; if(diff<60e3)return 'Только что'; if(diff<3600e3)return `${Math.floor(diff/60e3)} мин. назад`; const time=d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}); if(d.toDateString()===now.toDateString())return `Сегодня, ${time}`; const y=new Date(now); y.setDate(y.getDate()-1); if(d.toDateString()===y.toDateString())return `Вчера, ${time}`; return d.toLocaleString('ru-RU',{day:'numeric',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'}); }
export function downloadJson(data){ const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json;charset=utf-8'}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=`codebridge-${new Date().toISOString().slice(0,10)}.json`; a.click(); URL.revokeObjectURL(url); }
